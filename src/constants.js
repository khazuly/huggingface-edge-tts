export const DEFAULT_SPACE_ID = "innoai/Edge-TTS-Text-to-Speech";
export const DEFAULT_SPACE_URL =
  "https://innoai-edge-tts-text-to-speech.hf.space";
export const DEFAULT_API_NAME = "/tts_interface";
export const DEFAULT_VOICE = "id-ID-GadisNeural";
export const DEFAULT_TIMEOUT_MS = 120_000;

export const RATE_RANGE = Object.freeze({
  minimum: -50,
  maximum: 50,
});

export const PITCH_RANGE = Object.freeze({
  minimum: -20,
  maximum: 20,
});
